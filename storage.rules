rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Allow public read and write access for debugging purposes.
    // WARNING: This is insecure and should be locked down before production.
    match /{allPaths=**} {
      allow read, write;
    }
  }
}
